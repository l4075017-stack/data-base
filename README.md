# مشروع إدارة أدوية الأمراض المزمنة

هذا المشروع عبارة عن قاعدة بيانات لإدارة **أدوية المرضى المصابين بأمراض مزمنة**، مثل ضغط الدم والكوليسترول. يمكن تتبع المرض، الأدوية الموصوفة، مواعيد أخذ الدواء، والتنبيهات المتعلقة بالاحترازات.

---

## 📂 مكونات المشروع

### 1. جدول الأمراض المزمنة (`CHRONIC_DISEASES`)
- يخزن أسماء الأمراض المزمنة.
- أعمدة:
  - DISEASE_ID: معرف المرض (Primary Key)
  - DISEASE_NAME: اسم المرض

### 2. جدول الأدوية (`MEDICINES`)
- يحتوي على جميع الأدوية المرتبطة بكل مرض مزمن.
- أعمدة:
  - MEDICINE_ID: معرف الدواء (Primary Key)
  - MEDICINE_NAME: اسم الدواء
  - DISEASE_ID: معرف المرض المرتبط (Foreign Key)

### 3. جدول أوقات أخذ الدواء (`MEDICINE_SCHEDULE`)
- يحدد الوقت المناسب لكل دواء.
- أعمدة:
  - SCHEDULE_ID: معرف السطر (Primary Key)
  - MEDICINE_ID: معرف الدواء (Foreign Key)
  - TAKING_TIME: وقت أخذ الدواء (مثلاً صباحاً قبل الإفطار)

### 4. جدول التنبيهات / الاحترازات (`MEDICINE_ALERTS`)
- يخزن تنبيهات لكل دواء مثل موانع الاستخدام أو تحذيرات خاصة.
- أعمدة:
  - ALERT_ID: معرف السطر (Primary Key)
  - MEDICINE_ID: معرف الدواء (Foreign Key)
  - ALERT_NOTE: ملاحظة أو تنبيه

---

## 💾 طريقة الاستخدام

1. افتح Oracle APEX → SQL Workshop → SQL Scripts  
2. ارفع ملف chronic_medicine_project.sql  
3. اضغط Run Script لتنفيذ كل الجداول والإدخالات  
4. لتأكيد البيانات، نفذ الاستعلام التالي:

```sql
SELECT D.DISEASE_NAME,
       M.MEDICINE_NAME,
       S.TAKING_TIME,
       A.ALERT_NOTE
FROM MEDICINES M
JOIN CHRONIC_DISEASES D ON M.DISEASE_ID = D.DISEASE_ID
LEFT JOIN MEDICINE_SCHEDULE S ON M.MEDICINE_ID = S.MEDICINE_ID
LEFT JOIN MEDICINE_ALERTS A ON M.MEDICINE_ID = A.MEDICINE_ID
ORDER BY D.DISEASE_ID, M.MEDICINE_ID;
